server.port: 8888
spring:
  cloud:
     config:
        server:
          git:
            default-label: config-server
            uri: https://github.com/Jaimee2/Proyecto-Microservicios
            search-paths: config-data
  application:
    name: config-service